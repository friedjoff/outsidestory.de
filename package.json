{
  "name": "outsidestory.de",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "build": "parcel build src/index.js --public-url ./ --out-dir public",
    "watch": "parcel watch src/index.js --public-url ./ --out-dir static",
    "clean": "git clean -f static/"
  },
  "dependencies": {
    "normalize.css": "^8.0.0",
    "photoswipe": "^4.1.2"
  },
  "devDependencies": {
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.5.1"
  }
}
